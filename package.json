{
  "name": "improvlore",
  "version": "0.0.1",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "rm -rf _site && eleventy",
    "deploy": "node --env-file=.env scripts/deploy.js"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-fetch": "^4.0.0",
    "@11ty/eleventy-plugin-rss": "^3.0.0"
  },
  "devDependencies": {
    "@11ty/eleventy-img": "^6.0.4"
  }
}
